/**
 * An empty page.
 *
 * The Vite scaffold ships a demo — a counter, two logos, a paragraph about
 * HMR. An agent asked to build a plumber's site on top of that reliably
 * leaves some of it behind, and a stray Vite logo in the footer of a site you
 * are charging for is the kind of thing the client spots and you do not.
 */
export default function App() {
  return <main className="min-h-dvh" />;
}
